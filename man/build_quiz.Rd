% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build_quiz}
\alias{build_quiz}
\alias{build_question_bank}
\title{Build a quiz from a question-bank index}
\usage{
build_quiz(
  index_file,
  title = "Question Bank Quiz",
  ids = NULL,
  n = NULL,
  seed = NULL,
  topics = NULL,
  difficulties = NULL,
  category = "Question bank",
  replicates = 1,
  generated_dir = NULL,
  output_dir = NULL,
  render = TRUE
)

build_question_bank(
  index_file,
  title = "Question Bank Quiz",
  ids = NULL,
  n = NULL,
  seed = NULL,
  topics = NULL,
  difficulties = NULL,
  category = "Question bank",
  replicates = 1,
  generated_dir = NULL,
  output_dir = NULL,
  render = TRUE
)
}
\arguments{
\item{index_file}{Path to the authoritative question-index CSV. This is
deliberately required; see \code{vignette("getting-started")} for the design
rationale and \code{\link[=create_question_index]{create_question_index()}} for folder-based setup.}

\item{title}{Quiz title.}

\item{ids}{Optional ordered vector of question IDs.}

\item{n}{Optional random sample size. Do not combine with \code{ids}.}

\item{seed}{Optional seed for reproducible sampling.}

\item{topics, difficulties}{Optional filters applied before random sampling.}

\item{category}{Moodle question-bank category.}

\item{replicates}{Number of Moodle variants requested from \code{moodlequiz}.}

\item{generated_dir}{Folder for generated \code{.qmd} and \code{.Rmd} wrappers. \code{NULL}
uses \code{generated} next to the index.}

\item{output_dir}{Folder for the manifest and rendered outputs. \code{NULL} uses
\code{output} next to the index.}

\item{render}{If \code{FALSE}, create and validate sources without running
Quarto, Pandoc, or LaTeX.}
}
\value{
Invisibly, the selected rows of the question index.
}
\description{
Validates a complete bank, selects questions, writes wrapper sources and an
ordered manifest, and optionally renders Moodle XML plus student and teacher
HTML/PDF documents. Relative question paths are resolved from \code{index_file}.
Relative output directories are also resolved from the index's folder.
}
\examples{
root <- tempfile("ecosanctuary-example-")
dir.create(root)
selected <- build_quiz(
  index_file = example_question_index(),
  ids = "example-numeric",
  generated_dir = file.path(root, "generated"),
  output_dir = file.path(root, "output"),
  render = FALSE
)
selected$id
}
