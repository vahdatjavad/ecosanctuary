% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{create_question_index}
\alias{create_question_index}
\title{Create a question-index template from a folder}
\usage{
create_question_index(questions_dir, output_file = NULL, overwrite = FALSE)
}
\arguments{
\item{questions_dir}{Folder containing question fragments.}

\item{output_file}{Optional CSV path to write. If omitted, the template is
returned without writing a file.}

\item{overwrite}{Whether an existing \code{output_file} may be replaced.}
}
\value{
A data frame invisibly when written, and visibly otherwise.
}
\description{
Scans a folder recursively for \code{.qmd} and \code{.Rmd} question fragments. IDs
are inferred from filenames and titles from the first H2 heading. The
metadata that cannot be inferred safely is left as \code{NA}; complete those
fields before calling \code{\link[=validate_question_bank]{validate_question_bank()}}.
}
\examples{
questions <- system.file("extdata", "questions", package = "ecosanctuary")
template <- create_question_index(questions)
template[c("id", "file", "title")]
}
